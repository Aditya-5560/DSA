/*
 * Platform: GeeksforGeeks
 * Problem: Remove Cycle in Linked List
 * URL: https://www.geeksforgeeks.org/problems/remove-loop-in-linked-list/1
 * Language: Java (21)
 * Difficulty: Unknown
 * Topics: Expected Complexities, Company Tags, VMWare, Morgan Stanley, Amazon, Microsoft, Snapdeal, MakeMyTrip
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-09-06T18:53:36.089Z
 */

/* Structure of Linked List Node
class Node {
    int data;
    Node next;
    Node(int val) {
        data = val;
        next = null;
    }
} */
class Solution {
    public static void removeLoop(Node head) {
        //Detect Cycle
        Node slow = head;
        Node fast = head;
        boolean cycle = false;//No cycle
        while(fast!=null && fast.next!=null){
            slow = slow.next;
            fast = fast.next.next;
            if(slow==fast){
                cycle = true;
                break;
            }
        }
        if(!cycle){//If no cycle exit
            return;
        }
        //find meeting point
        slow = head;
        while(slow!=fast){
            slow=slow.next;
            fast=fast.next;
        }
        Node temp = slow;
        while(temp.next!=slow){
            temp = temp.next;
        }
        //remove cycle 
        temp.next = null;
        return;
    }
}
